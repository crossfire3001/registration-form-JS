window.onload = function () {


    /* if (!orderFilled) {
    [productInput, addressInput, phoneInput].forEach((item) => {
        item.value = "";
    });
    alert("Спасибо за заказ!");
} */

    /* baseInputs.forEach((input) => {
         if(!inputFilled.value) {
             console.log('Заполните все поля');
         }
    });

     if (!inputFilled) {
         baseInputs.forEach((item) => {
             item.value = "";
         });
         alert("Спасибо за заказ!");
     } */
}